services:
  python-app:
    image: python:3.12-slim
    container_name: python312-container
    working_dir: /code
    volumes:
      - ./code:/code
      - ./requirements.txt:/code/requirements.txt
    command: >
      sh -c "pip install --root-user-action --upgrade pip &&
             pip install --root-user-action --no-cache-dir -r requirements.txt &&
             python main.py"
    networks:
      - clickhouse-net

networks:
  clickhouse-net:
    external: true
